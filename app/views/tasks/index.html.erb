<div class="px-auto text-center py-8">
<h1> <%= current_user.first_name %> </h1>


<h3> Tâches du jour </h3>
</div>

<div class="flex flex-col gap-6 px-8">
  <% current_user.assignations.each do | assignation | %>
    <% if assignation.statut == false && assignation.date == Date.today %>
  <div class="flex justify-between items-center py-4 pl-4 bg-blue-200 shadow-lg">
    <%= cl_image_tag current_user.photo.key, class:"mr-4 border-solid border-2 border-blue-200 w-12 h-12 rounded-full" %>
    <span class="text-black font-bold text-sm uppercase tracking-wide"><%= assignation.task.name %></span>
    <%= simple_form_for assignation do |f| %>
    <%= f.button :submit , "Valider", class:"mr-4 inline-block px-2 py-2 rounded-lg bg-yellow-500 text-white uppercase tracking-wide font-semibold text-xs" %>
    <% end %>
  </div>


  <% end %>
  <% end %>
</div>



<div class="px-auto text-center py-8">
  <h3> Tâche de <%= Date.today.next_day.strftime("%A") %> </h3>

  <div class="flex flex-col gap-6 px-8">
    <% current_user.assignations.each do | assignation | %>
      <% if assignation.statut == false && assignation.date == Date.today.next_day %>
  <div class="flex justify-between items-center py-4 pl-4 bg-blue-200 shadow-lg">
    <%= cl_image_tag current_user.photo.key, class:"mr-4 border-solid border-2 border-blue-200 w-12 h-12 rounded-full" %>
    <span class="text-black font-bold text-sm uppercase tracking-wide"><%= assignation.task.name %></span>
    <%= simple_form_for assignation do |f| %>
    <%= f.button :submit , "Valider", class:"mr-4 inline-block px-2 py-2 rounded-lg bg-yellow-500 text-white uppercase tracking-wide font-semibold text-xs" %>
    <% end %>
  </div>
      <% end %>
    <% end %>
  </div>
</div>

<div class="px-auto text-center py-8">
  <h3> Tâche de <%= Date.today.next_day(2).strftime("%A") %> </h3>

  <div class="flex flex-col gap-6 px-8">
    <% current_user.assignations.each do | assignation | %>
      <% if assignation.statut == false && assignation.date == Date.today.next_day(2)%>
  <div class="flex justify-between items-center py-4 pl-4 bg-blue-200 shadow-lg">
    <%= cl_image_tag current_user.photo.key, class:"mr-4 border-solid border-2 border-blue-200 w-12 h-12 rounded-full" %>
    <span class="text-black font-bold text-sm uppercase tracking-wide"><%= assignation.task.name %></span>
    <%= simple_form_for assignation do |f| %>
    <%= f.button :submit , "Valider", class:"mr-4 inline-block px-2 py-2 rounded-lg bg-yellow-500 text-white uppercase tracking-wide font-semibold text-xs" %>
    <% end %>
  </div>
      <% end %>
    <% end %>
  </div>

</div>

<div class="px-auto text-center py-8">
  <h3> Tâche de <%= Date.today.next_day(3).strftime("%A") %> </h3>

  <div class="flex flex-col gap-6 px-8">
    <% current_user.assignations.each do | assignation | %>
      <% if assignation.statut == false && assignation.date == Date.today.next_day(3) %>
  <div class="flex justify-between items-center py-4 pl-4 bg-blue-200 shadow-lg">
    <%= cl_image_tag current_user.photo.key, class:"mr-4 border-solid border-2 border-blue-200 w-12 h-12 rounded-full" %>
    <span class="text-black font-bold text-sm uppercase tracking-wide"><%= assignation.task.name %></span>
    <%= simple_form_for assignation do |f| %>
    <%= f.button :submit , "Valider", class:"mr-4 inline-block px-2 py-2 rounded-lg bg-yellow-500 text-white uppercase tracking-wide font-semibold text-xs" %>
    <% end %>
  </div>
      <% end %>
    <% end %>
  </div>

</div>

<div class="px-auto text-center py-8">
  <h3> Tâche de <%= Date.today.next_day(4).strftime("%A") %> </h3>

  <div class="flex flex-col gap-6 px-8">
    <% current_user.assignations.each do | assignation | %>
      <% if assignation.statut == false && assignation.date == Date.today.next_day(4) %>
  <div class="flex justify-between items-center py-4 pl-4 bg-blue-200 shadow-lg">
    <%= cl_image_tag current_user.photo.key, class:"mr-4 border-solid border-2 border-blue-200 w-12 h-12 rounded-full" %>
    <span class="text-black font-bold text-sm uppercase tracking-wide"><%= assignation.task.name %></span>
    <%= simple_form_for assignation do |f| %>
    <%= f.button :submit , "Valider", class:"mr-4 inline-block px-2 py-2 rounded-lg bg-yellow-500 text-white uppercase tracking-wide font-semibold text-xs" %>
    <% end %>
  </div>
      <% end %>
    <% end %>
  </div>

</div>

<div class="px-auto text-center py-8">
  <h3> Tâche de <%= Date.today.next_day(5).strftime("%A") %> </h3>

  <div class="flex flex-col gap-6 px-8">
    <% current_user.assignations.each do | assignation | %>
      <% if assignation.statut == false && assignation.date == Date.today.next_day(5) %>
  <div class="flex justify-between items-center py-4 pl-4 bg-blue-200 shadow-lg">
    <%= cl_image_tag current_user.photo.key, class:"mr-4 border-solid border-2 border-blue-200 w-12 h-12 rounded-full" %>
    <span class="text-black font-bold text-sm uppercase tracking-wide"><%= assignation.task.name %></span>
    <%= simple_form_for assignation do |f| %>
    <%= f.button :submit , "Valider", class:"mr-4 inline-block px-2 py-2 rounded-lg bg-yellow-500 text-white uppercase tracking-wide font-semibold text-xs" %>
    <% end %>
  </div>
      <% end %>
    <% end %>
  </div>

</div>
(6)
<div class="px-auto text-center py-8">
  <h3> Tâche de <%= Date.today.next_day(6).strftime("%A") %> </h3>

  <div class="flex flex-col gap-6 px-8">
    <% current_user.assignations.each do | assignation | %>
      <% if assignation.statut == false && assignation.date == Date.today.next_day(6) %>
  <div class="flex justify-between items-center py-4 pl-4 bg-blue-200 shadow-lg">
    <%= cl_image_tag current_user.photo.key, class:"mr-4 border-solid border-2 border-blue-200 w-12 h-12 rounded-full" %>
    <span class="text-black font-bold text-sm uppercase tracking-wide"><%= assignation.task.name %></span>
    <%= simple_form_for assignation do |f| %>
    <%= f.button :submit , "Valider", class:"mr-4 inline-block px-2 py-2 rounded-lg bg-yellow-500 text-white uppercase tracking-wide font-semibold text-xs" %>
    <% end %>
  </div>
      <% end %>
    <% end %>
  </div>

</div>

<%= render 'shared/navigation_footer' %>
